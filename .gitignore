.venv*/
__pycache__/
build/
dist/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
private/

# generated ZFT artifacts (specs/ and contracts/ remain tracked)
.zft/gherkin/
.zft/runs/
.zft/sandbox*/
.zft/cache/
.zft/baseline/
.zft/attest.json
.zft/attest-key.pub.json
.zft/audit.log
.zft/tmp_oracle_batch.*
.mutmut*
# mutation-campaign workspaces, incl. renamed archives (mutants-prev-*) —
# never evidence, see lineage.extract.skipped_dir and ATTACK_MATRIX.md EVID-1
mutants*/

# opencode plugin dev scaffolding — the TS plugin's @opencode-ai/plugin
# import is type-only (stripped at transpile; opencode loads it raw), so these
# exist only for local type-checking. They never ship: PUBLIC_REPO.md ships
# .opencode/{plugins,skills} + README only.
.opencode/node_modules/
.opencode/package.json
.opencode/package-lock.json

# Phase-B batch artifacts (jobs/README.md)
scratch/
jobs/out-*.log
jobs/err-*.log
jobs/done-*
jobs/.*.lock
jobs/junit-*.xml
jobs/hyp-db/
jobs/kick.log
jobs/
texput.log
.slim/deepwork/

# paper review pipeline transient state
paper/review/state/
