"""zft — contract-first traceability for multiagent work.

Dependency direction (plan §2): spec <- dsl <- codegen <- gates <- (attest,
negotiate); lineage feeds gates; debug depends on nothing above it.
"""

__version__ = "0.2.0a1"
